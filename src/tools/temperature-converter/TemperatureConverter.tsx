import { ToolPlaceholder } from "../ToolPlaceholder";

/**
 * Temperature converter (REQ-6).
 *
 * Stub: the registry entry and the URL exist from this ticket onwards; the
 * screen itself is built in its own ticket, which replaces the placeholder.
 */
export function TemperatureConverter() {
  return <ToolPlaceholder toolName="Temperature Converter" />;
}
