import { ToolPlaceholder } from "../ToolPlaceholder";

/**
 * Weight unit converter (REQ-5).
 *
 * Stub: the registry entry, the URL and the page around it exist from this
 * ticket onwards; the conversion core and this screen are built in TKT-10 and
 * TKT-11, which replace the placeholder below.
 */
export function WeightConverter() {
  return <ToolPlaceholder toolName="Weight Converter" />;
}
